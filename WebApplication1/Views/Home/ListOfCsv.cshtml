@model string[]

<div class="panel panel-primary">
    <div class="panel-heading">Файлы CSV</div>
    <div class="panel-body">
        <ul class="nav nav-pills nav-stacked">
            @foreach (var item in @Model)
            {
                <li role="presentation"> @Html.ActionLink(@item, "ItemsTable", new { id = @item })</li>
            }
        </ul>
    </div>

</div>